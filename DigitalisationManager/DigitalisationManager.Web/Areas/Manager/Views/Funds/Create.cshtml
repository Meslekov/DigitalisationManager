@using DigitalisationManager.Web.ViewModels.Funds

@model FundFormViewModel

@{
    ViewData["Title"] = "Create Fund";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h1 class="h4 mb-0">Create Fund</h1>
        <div class="text-muted small">Add a new archival grouping</div>
    </div>
    <a class="btn btn-outline-secondary" asp-action="Index">Back</a>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <partial name="_FundFormPartial" model="Model" />

            <div class="mt-4 d-flex gap-2">
                <button type="submit" class="btn btn-dark">Save</button>
                <a class="btn btn-outline-secondary" asp-action="Index">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
