@{
    ViewData["Title"] = "Page Not Found";
}

<div class="container py-5">
    <div class="mx-auto text-center" style="max-width: 700px;">
        <div class="display-1 fw-bold text-secondary">404</div>

        <h1 class="h3 mb-3">Page not found</h1>

        <p class="text-muted mb-4">
            The page you are looking for does not exist, was moved, or the address is incorrect.
        </p>

        <div class="d-flex justify-content-center gap-2 flex-wrap">
            <a asp-controller="Home"
                   asp-action="Index"
                   class="btn btn-primary">
                Go to Home
            </a>

            <button type="button"
                    class="btn btn-outline-secondary"
                    onclick="history.back()">
                Go Back
            </button>
        </div>
    </div>
</div>